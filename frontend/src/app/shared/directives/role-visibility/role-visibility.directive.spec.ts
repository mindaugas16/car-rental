import { RoleVisibilityDirective } from './role-visibility.directive';

describe('RoleVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
