import { RentStatusPipe } from './rent-status-pipe.pipe';

describe('RentStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RentStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
